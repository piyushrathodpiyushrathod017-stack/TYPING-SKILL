# Cloudflare Pages redirect rules
# Custom 404 handled by wrangler.jsonc (not_found_handling: 404-page)

# Legacy shortcuts
/11  /admin-login.html  301

# Ensure clean URLs
/index.html  /  301

# Direct extensionless access — Cloudflare Pages auto-serves .html files.
# No rewrite rules needed; they conflict with auto-detection and create redirect loops.
